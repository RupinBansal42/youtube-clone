import { createSlice } from "@reduxjs/toolkit";


const searchSlice = createSlice({
    name: "search",
    initialState: null,
    reducers: {
        cachedResult : (state,action)  => {
        }
    } 
})